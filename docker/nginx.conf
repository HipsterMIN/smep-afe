user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    # gzip  on; # 필요시 주석 해제하여 사용

    server {
        listen       80;
        server_name  localhost;

        # [중요] 빌드된 정적 파일(React/Vue 등)이 위치한 경로
        # Docker 공식 Nginx 이미지의 기본 경로는 /usr/share/nginx/html 입니다.
        root   /usr/share/nginx/html;
        index  index.html index.htm;

        # [요청하신 부분] SPA 라우팅 설정
        # 1. $uri: 요청한 파일이 실제로 있는지 확인
        # 2. $uri/: 요청한 디렉토리가 있는지 확인
        # 3. /index.html: 둘 다 없으면 index.html로 내부 리다이렉트 (Client Side Routing이 처리)
        location / {
            try_files $uri $uri/ /index.html;
        }

        # 백엔드 API 프록시 설정
        location /main-dev/ {.
            proxy_pass http://smep-admin-dev.smepbe:8082;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_cache_bypass $http_upgrade;
        }

        # 500 에러 처리 (선택 사항)
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   /usr/share/nginx/html;
        }
    }
}
