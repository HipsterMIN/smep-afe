// ============================================
// A simple wrapper component that renders its children.
// ============================================

// eslint-disable-next-line react/prop-types
export default function Contents({ children }) {
  return <>{children}</>;
}